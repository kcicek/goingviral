import { defineConfig } from 'vite';

// Configure Vite for GitHub Pages project site
// When hosted at https://kcicek.github.io/goingviral/
// asset URLs must be prefixed with /goingviral/
export default defineConfig({
  base: '/goingviral/',
});
